<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MemoryGame" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/redy</item>
        <item name="colorPrimaryVariant">@color/greeny</item>
        <item name="colorOnPrimary">@color/pinky</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/browny</item>
        <item name="colorSecondaryVariant">@color/peachy</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>